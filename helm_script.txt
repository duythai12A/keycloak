helm install keycloak ./bitnami/keycloak --kube-insecure-skip-tls-verify --namespace dev --kubeconfig ./kubeconf/local.yaml `
--set clusterDomain=cluster.local `
--set image.registry=docker.io `
--set image.repository=bitnami/keycloak `
--set image.tag=26.1.0-debian-12-r0 `
--set auth.adminUser=admin `
--set auth.adminPassword=admin `
--set service.nodePort.http=30005 `
--set resources.requests.cpu=2 `
--set resources.requests.memory=512Mi `
--set resources.limits.cpu=2 `
--set resources.limits.memory=512Mi `
--set postgresql.enabled=false `
--set externalDatabase.host=172.23.0.127 `
--set externalDatabase.port=30225 `
--set externalDatabase.user=thaipd `
--set externalDatabase.database=spring-project `
--set externalDatabase.password=123456aA@ `
--set extraVolumes[0].name=keycloak-data-volume `
--set extraVolumes[0].persistentVolumeClaim.claimName=keycloak-pvc `
--set extraVolumeMounts[0].name=keycloak-data-volume `
--set extraVolumeMounts[0].mountPath=/opt/bitnami/keycloak/data `
--set global.defaultStorageClass=local-path `
--set metrics.enabled=true `
--set metrics.service.ports.http=8080 `
--set metrics.service.ports.metrics=9000 `
--set metrics.serviceMonitor.enabled=true `
--set metrics.serviceMonitor.namespace=cattle-monitoring-system `
--set global.storageClass=local-path